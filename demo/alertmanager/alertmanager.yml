global:
  resolve_timeout: 10s

route:
  receiver: demo-webhook
  group_by: [alertname, service]
  group_wait: 2s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: demo-webhook
    webhook_configs:
      - url: http://checkout-1:8080/alerts
        send_resolved: true
