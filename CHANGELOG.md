# Changelog

All notable changes will be documented here. The project follows Semantic Versioning.

## Unreleased

### Added

- PromQL AST mutations for comparisons, thresholds, ranges, aggregations, functions, selectors, and logical operators.
- Alert `for` duration mutations.
- Isolated, concurrent execution through the official `promtool` checker and unit-test runner.
- Mutation score gates and terminal, JSON, JUnit, SARIF, and standalone HTML reports.
- Survivor persistence, operator filters, timeouts, deterministic IDs, and a validated example corpus.
